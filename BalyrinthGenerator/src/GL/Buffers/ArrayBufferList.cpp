#include "ArrayBufferList.h"

ArrayBufferList::ArrayBufferList(uint32_t pBufferCount, const BufferUsage* pBufferUsage):
    mBufferCount(pBufferCount)
{
    for (uint32_t i = 0; i < pBufferCount; ++i)
    {

    }
}

ArrayBufferList::~ArrayBufferList()
{

}
